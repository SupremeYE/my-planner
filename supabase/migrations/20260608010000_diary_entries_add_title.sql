-- =====================================================================
-- 통합 일기 — 자유일기 제목(title) 컬럼 추가
-- ---------------------------------------------------------------------
-- 오늘 일기(자유일기)에 선택적 제목을 둔다. 질문일기는 질문이 제목 역할을
-- 하므로 사용하지 않는다(NULL 허용). 같은 행 정책(RLS)을 그대로 따른다.
-- =====================================================================
alter table public.diary_entries add column if not exists title text;
